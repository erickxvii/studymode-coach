/** @type {import('next').NextConfig} */
const nextConfig = {
  printf '/** @type {import("next").NextConfig} */\nconst nextConfig = {};\n\nexport default nextConfig;\n' > next.config.mjs

};

export default nextConfig;

